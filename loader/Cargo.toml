[package]
name = "loader"
version = "0.1.0"
edition = "2021"

[dependencies]
aya = "0.13.1"  # Only core aya needed
anyhow = "1.0"
clap = { version = "4.0", features = ["derive"] }
tokio = { version = "1.0", features = ["full"] }