[package]
name = "xdp_router"
version = "0.1.0"
edition = "2021"

[dependencies]
aya = "0.18"  # Modern eBPF lib (2025 standard)
aya-log-ebpf = "0.18"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"