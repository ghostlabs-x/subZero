[package]
name = "xdp_router"
version = "0.1.0"
edition = "2021"

[dependencies]
aya = "0.13.1"  # Latest stable
aya-log-ebpf = "0.13.1"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "abort"